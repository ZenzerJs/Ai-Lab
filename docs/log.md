# Knowledge Bundle Update Log

## 2026-09-09 (Update 3)
* **Gemini Rate-Card Correction (audit)**: Verified `config/PRICING.json` against the official Google AI pricing page and corrected three stale entries: `gemini-3.8-flash` ($0.075/$0.01875/$0.30 → **$0.75/$0.075/$3.75** per M standard paid tier, promotional through Dec 31 2026), `gemini-2.5-flash` ($0.075/$0.01875/$0.30 → **$0.30/$0.03/$2.50**), and `gemini-2.5-pro` (cache $0.3125 → **$0.125**, output $5.00 → **$10.00**; the old output figure matched the Batch tier). Claude, GPT-4o, and GLM cards verified correct. Re-seeded the ledger; all cascade and cumulative figures recomputed: measured headline savings rise from 52.8% to **61.58%** ($0.37910 baseline vs. $0.14566 ICM across 16 runs), and `gemini-3.8-flash` joins the Claude tiers at 61.6% simulation savings thanks to its matching 90% cache discount. README cascade table/chart, header stats, per-task savings, and verification-ledger percentages updated; `experiments/README.md` pricing example refreshed; dashboard `data.json` regenerated.
## 2026-09-09 (Update 2)
* **glm-5.3-flash Provider-Equivalent Pricing & EXP-005 Benchmark Group**: Added `glm-5.3-flash` to `config/PRICING.json` ($0.075/$0.015/$0.25 per M, source: OpenRouter) with new `pricing_mode: "provider-equivalent"` and `provider_note` fields now persisted in the SQLite pricing schema (auto-migrated for existing databases) and surfaced through `dashboard/build_data.py`, the Model Rate Card Simulator ("Provider-equivalent · $0 direct cost" badge), and the Spend Cascade table ("Provider-equivalent · simulated" badge plus methodology note). Created `experiments/tasks/EXP-005-G1..G4.md` reusing the EXP-001–004 prompts verbatim with distinct task IDs (ledger isolation from measured Gemini totals) and `experiments/EXP-005-protocol.md` documenting the $0-direct-cost vs provider-equivalent-cost separation. GLM dollar figures are simulations of public API pricing — FreeBuff direct user cost is $0 — and are excluded from measured savings totals. Live EXP-005 runs pending.
* **Documentation Accuracy Fixes**: Corrected MCP filesystem sandbox description (4 roots → 8 roots: adds `./data`, `./experiments`, `./dashboard`, `./config`) in `AGENTS.md` and `docs/concepts/architecture-overview.md`, replaced the stale "net-new application logic lives strictly under `src/`" claim with the actual tooling layout (`scripts/`, `dashboard/`), fixed an out-of-scale gpt-4o bar in the README ASCII spend chart, and stripped a stray UTF-8 BOM from `experiments/tasks/EXP-001.md`.

## 2026-09-09
* **Claude Sonnet Highlights, Model Disclaimer & Test Isolation**: Updated README and dashboard with prominent highlights for Claude Sonnet 4.6 and Sonnet 5 simulated economics (61.58% cost reduction, +$2,076.25 saved @ 1B tokens), expanded verification ledger to 19 verified tests (correcting AST extraction target to `filter_output.py`), added prominent disclaimers distinguishing live empirical trials (`gemini-3.8-flash`) from rate-card simulations (`claude-sonnet-4-6`, `claude-sonnet-5`, `claude-3-7-sonnet`, `gpt-4o`, `gemini-2.5-pro`, `gemini-2.5-flash`) without live commercial API calls, and isolated synthetic mock test fixtures from cumulative analytics and dashboard data exports with `--db` CLI support.
* **claude-sonnet-4-6 & claude-sonnet-5 Rate Cards**: Added both models to `config/PRICING.json` and SQLite pricing table. Sonnet 4.6 ($3.00/$0.30/$15.00 per M) matches claude-3-7-sonnet tier — 61.6% ICM savings, +$2,076 @ 1B tokens. Sonnet 5 ($2.00/$0.20/$10.00 per M) shows 61.6% savings, +$1,384 @ 1B tokens. Both available in dashboard Model Rate Card Simulator and CLI cascade.
* **GitHub Pages Live Dashboard**: Added `.github/workflows/deploy-dashboard.yml` CI deploy workflow; configured Vite `base` path (`VITE_BASE_PATH=/Ai-Lab/`) for sub-path routing. Dashboard auto-deploys to [zenzerjs.github.io/Ai-Lab](https://zenzerjs.github.io/Ai-Lab/) on every push to `main`.
* **Multi-Model Spend Cascade & Empirical Benchmarks**: Executed empirical trials (`EXP-001` through `EXP-004`, 16 runs on `gemini-3.8-flash`, 52.82% cost reduction). Added dynamic Foundation Model Rate Card simulation (`gemini-3.8-flash`, `gemini-2.5-pro`, `gpt-4o`, `claude-3-7-sonnet`) and multi-model spend cascade visualizer across 1x to 1B token scale.
* **Measurement Layer**: Implemented A/B experiment runner (`scripts/run_experiment.py`), SQLite usage ledger (`scripts/ledger.py`), local Vite + React savings dashboard (`dashboard/`), and measurement layer OKF concept ([Measurement Layer](/concepts/measurement-layer.md)).
* **Initialization**: Bootstrapped OKF v0.2 knowledge bundle, architecture overview, coding standards, and specification version tracking.